<h1>Create a bookmark for <%= @list.name %> Movie List</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :movie_id, collection: Movie.all %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>

<%= link_to "Back To Movies", list_path(@list), class: "btn btn-dark mt-2" %>
